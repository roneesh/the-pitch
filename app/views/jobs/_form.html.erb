<%= form_for @job do |j| %>
  <%= j.label :title%>
  <%= j.text_field :title%>
  
  <%= j.label :description%>
  <%= j.text_field :description%>
  
  <%= j.label :maxpitchcount%>
  <%= j.text_field :maxpitchcount%>
  
<!--   taken out because employer_id should assign via session has -->h
  <%#= j.label :employer_id %>
  <%#= j.collection_select :employer_id, Employer.all, :id, :name %>
  
  <%= j.submit "Create Job!"%>
<% end %>