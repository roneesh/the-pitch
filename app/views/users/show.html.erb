<p><strong>User Name:</strong><br>
  <%= @user.first_name %> <%= @user.last_name%>  
</p>

<p><strong>Contact Info:</strong><br>
  <%= @user.email%>  
</p>

<p><strong><%= link_to "Edit User Information", edit_user_url(@user.id) %></strong><br></p>

<p><strong>Existing Pitches:</strong><br>
  <ul>
  <% @user.pitches.each do |pitch_object| %>
  		<li> <%= link_to pitch_object.headline, pitch_url(pitch_object.id) %> |
  			 <%= link_to "Edit", edit_pitch_url(pitch_object.id) %> | 
  			 <%= link_to "Delete", pitch_url(pitch_object.id), html_options = {method: 'delete', confirm: "Are you sure? Deleted Pitches can not be retrieved."} %>
  		</li>
  <%end%>
</ul>
</p>


<p><strong>LinkedIn and Facebook Settings:</strong><br>
  We need to add and manage LinkedIn and Facebook Permissions from this area.
</p>
