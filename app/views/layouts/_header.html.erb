<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="<%= root_url %>">ThePitch</a>
      
    <ul class="nav pull-right">
      <% if session[:user_id]==nil && session[:employer_id] == nil %>
      <li><%= link_to "Job Seeker Login", userlogin_path %></li>
      <li><%= link_to "Employer Login", employerlogin_path %></li>
   	
      <% elsif Employer.find_by_id(session[:employer_id])%>
        <li class="divider-vertical"><%= link_to "Dashboard", employer_url(session[:employer_id]) %></li>
        <li class="divider-vertical"><%= link_to "New Job", new_job_url %></li>
        <li class="divider-vertical"><%= link_to "Jobs Listing", jobs_url %></li>
        <li class="divider-vertical"><%= link_to "Logout", logout_url%></li>
      
      <% elsif User.find_by_id(session[:user_id])%>
        <li class="divider-vertical"><%= link_to "Dashboard", user_url(session[:user_id]) %></li>
        <li class="divider-vertical"><%= link_to "Jobs Listing", jobs_url %></li>
        <li class="divider-vertical"><%= link_to "New Pitch", new_pitch_url %></li>
        <li class="divider-vertical"><%= link_to "Logout", logout_url%></li>
      <%end%>
    </ul>

  </div>
</div>

